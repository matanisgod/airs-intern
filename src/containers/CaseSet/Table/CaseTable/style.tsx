import { styled, Box } from '@mui/material';

export const CaseTableBox = styled(Box)(() => ({
  width: '100%',
  height: '50%',
  display: 'flex',
  alignSelf: 'flex-end',
  flexDirection: 'column',
  paddingBottom: '5px',
}));
