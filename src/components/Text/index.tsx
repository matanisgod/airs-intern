import { styled, Typography } from '@mui/material';

export const ExecutionText = styled(Typography)(() => ({
  fontSize: '10rem',
  color: 'red',
  fontWeight: 'normal',
  marginRight: 30,
  display: 'flex',
}));
