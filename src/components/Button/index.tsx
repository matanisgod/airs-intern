import { Button, styled } from '@mui/material';

export const ExampleButton = styled(Button)(() => ({
  fontSize: '24px',
  color: '#f2f2f2',
  fontWeight: 'normal',
  marginRight: 30,
  display: 'flex',
}));