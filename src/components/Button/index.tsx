import { Button, styled } from '@mui/material';

export const PageButton = styled(Button)(() => ({
  fontSize: '24px',
  color: 'black',
  fontWeight: 'normal',
  right: '30px',
  top: '25px',
  position: 'fixed',
  background: 'white',
  textTransform: 'none',
}));
