import { Box, styled } from '@mui/material';

export const HeaderBox = styled(Box)(() => ({
  fontSize: '40px',
  color: 'white',
  fontWeight: 'bolder',
  left: '40px',
  top: '25px',
  position: 'fixed',
}));
